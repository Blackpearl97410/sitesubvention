[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"
  NETLIFY_NEXT_SKEW_PROTECTION = "true"
  SECRETS_SCAN_OMIT_KEYS = "RESEND_FROM_EMAIL"

[functions."refresh-aides-ouvertes"]
  schedule = "0 6 * * 1"
